{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/kiff/kiff/skills/kiff-audit/evidence-schema.json",
  "title": "KIFF audit evidence contract",
  "description": "Language-neutral shape that deterministic evidence collectors emit for an audit to consume. Go: `kiff scan -format json`. Python: the separately published kiff-scan package. Other languages: any collector that conforms. The audit skill treats this as evidence, never as a verdict.",
  "type": "object",
  "required": ["schema_version", "collector", "target", "findings"],
  "properties": {
    "schema_version": { "type": "string", "const": "1" },
    "collector": {
      "type": "object",
      "required": ["name", "version", "language"],
      "properties": {
        "name": { "type": "string", "examples": ["kiff scan", "kiff-scan"] },
        "version": { "type": "string" },
        "language": { "type": "string", "examples": ["go", "python", "typescript"] }
      }
    },
    "target": {
      "type": "object",
      "required": ["path"],
      "properties": {
        "path": { "type": "string" },
        "revision": { "type": "string", "description": "Commit sha or `git describe` output. Required for a reproducible audit." }
      }
    },
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["rule_id", "severity", "category", "file", "line"],
        "properties": {
          "rule_id": { "type": "string" },
          "severity": { "enum": ["low", "medium", "high"] },
          "category": {
            "description": "Shared sink taxonomy. Collectors in every language MUST use these values so findings are comparable across a polyglot repository.",
            "enum": [
              "money movement",
              "data loss",
              "access control",
              "code execution",
              "infrastructure",
              "external communication"
            ]
          },
          "file": { "type": "string" },
          "line": { "type": "integer", "minimum": 1 },
          "tool": { "type": "string", "description": "Model-reachable entry point that reaches the sink." },
          "call": { "type": "string", "description": "The consequential call itself." },
          "governed": { "type": "boolean", "description": "Whether a recognized decision boundary was found on the path. Absence of a boundary is not proof of exploitability." }
        }
      }
    },
    "coverage": {
      "type": "object",
      "description": "What the collector could and could not analyze. The audit reports these as `not assessable` rather than as passes.",
      "properties": {
        "files_scanned": { "type": "integer" },
        "unsupported_paths": { "type": "array", "items": { "type": "string" } },
        "limits": { "type": "array", "items": { "type": "string" } }
      }
    }
  }
}
